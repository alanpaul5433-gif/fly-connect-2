{
  "name": "functions",
  "description": "FlyConnect Cloud Functions — notification producers (C-3) + push fan-out",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions,firestore",
    "test": "jest",
    "test:integration:run": "jest --config jest.integration.config.js",
    "test:integration": "npm run build && cd .. && firebase emulators:exec --only firestore,functions --project demo-flyconnect-test \"npm --prefix functions run test:integration:run\"",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "firebase-admin": "^13.5.0",
    "firebase-functions": "^7.2.5"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.14.0",
    "firebase-functions-test": "^3.5.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.6.0"
  }
}
