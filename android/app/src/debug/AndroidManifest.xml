<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The INTERNET permission is required for development. Specifically,
         the Flutter tool needs it to communicate with the running application
         to allow setting breakpoints, to provide hot reload, etc.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- DEBUG-ONLY: allow cleartext HTTP to the Firebase Emulator Suite so the
         Android E2E harness can reach the Auth/Firestore/Storage emulators.
         Scoped to the emulator hosts by the referenced config; merged only into
         debug builds, so release manifests never see it. -->
    <application android:networkSecurityConfig="@xml/network_security_config"/>
</manifest>
